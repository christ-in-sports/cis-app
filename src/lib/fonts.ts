import { Caprasimo, Figtree } from 'next/font/google';

export const caprasimo = Caprasimo({ subsets: ['latin'], weight: '400', variable: '--cis-font-display' });
export const figtree = Figtree({ subsets: ['latin'], variable: '--cis-font-body' });
